# need to determine which columns from CAMPSTAAR2 (now achievement) we want to keep
# work in favor of keeping more rather than cutting too severely

# columns of interest:

achievement %<>%
  select(CAMPUS,
         )